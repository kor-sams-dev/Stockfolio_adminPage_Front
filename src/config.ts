export const BASE_URL = "https://api-we.stockfolio.ai";

export const Recruits = `${BASE_URL}/recruits`;

export const SignIn = `${BASE_URL}/users/signin`;

export const TEST_URL = "http://192.168.35.189:8000";

export const LoginAdmin = `${TEST_URL}/users/signin`;

export const RecruitAdmin = `${TEST_URL}/recruits/admin`;

export const Applications = `${TEST_URL}/applications`;

export const ApplicationsAdmin = `${Applications}/admin`;

export const NotificationUrl = `${TEST_URL}/recruits/user`;

export const Account = `${TEST_URL}/users/admins`;
