export interface IAccountItem {
  id: number;
  title: string;
  name: string;
  placeholder: string;
  itemWidth: number;
}

export interface INotificationItem {
  id: number;
  title: string;
  itemWidth: number;
}
